package com.yelle233.liubai.api;

/** Opt-in render quality recommendation. Renderers remain responsible for safe visual degradation. */
public enum RenderQuality {
    FULL,
    REDUCED,
    MINIMAL
}
